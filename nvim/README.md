# Nvim

### Note: Nvim mason (linting, lsp, formatting, dap) needs npm and python virtual environment

sudo apt install npm
sudo apt install python3-venv

or 

use local setup.sh script to install and stow!

```bash
$ setup.sh
```
