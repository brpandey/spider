## SPIDER

```bash
./setup_all.sh
```

or, individually with each module (installs pkgs and stows)

```bash
./bash/setup.sh
./helix/setup.sh
```

### Deprecated

> sudo apt install stow
> stow -t ~ pkg_name

```bash
 stow -t ~ fonts
 stow -t ~ nvim
 stow -t ~ helix
 stow -t ~ polybar
 stow -t ~ rofi
 stow -t ~ i3
 stow -t ~ tmux
```
